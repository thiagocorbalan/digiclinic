<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs'); %>
<body>
    <% if (users.length > 0) { %>
		<% users.forEach(user => { %>
			<h3><%= user.firstName %> <%= user.lastName %></h3>
            <p><%= user.email %></p>
		<% }) %>
	<% } %>
</body>
</html>